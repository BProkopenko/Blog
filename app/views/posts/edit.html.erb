<%= form_for( @post ) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :topic_id %>
  <%= f.select(:topic_id, options_for_select(@topics)) %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit "Save" %>
<% end %>